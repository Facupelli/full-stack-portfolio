---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const defaultClass =
  "py-4 px-6 border border-[rgba(255,255,255,0.3)] rounded-[32px] leading-none";
---

<div class:list={[defaultClass, className]}>
  <slot />
</div>
