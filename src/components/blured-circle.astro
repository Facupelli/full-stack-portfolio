---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const defaultClass =
  "intro-background absolute left-1/2 top-1/2 lg:top-[40%] rounded-full blur-[150px] opacity-50 size-[500px] bg-fuchsia-500";
---

<div class:list={[defaultClass, className]}></div>
